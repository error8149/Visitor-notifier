<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp">

    <ImageView
        android:id="@+id/dhlLogoImageView"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:contentDescription="@string/dhl_logo_desc"
        android:src="@drawable/dhl_logo"
        android:scaleType="centerInside"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/agreementTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/agreement_title"
        android:textSize="24sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:paddingBottom="16dp"
        android:textColor="@color/text_primary" />

    <ScrollView
        android:id="@+id/agreementScrollView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <TextView
            android:id="@+id/agreementTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/agreement_points"
            android:textSize="16sp"
            android:textColor="@color/text_primary"
            android:lineSpacingExtra="6dp"
            android:padding="12dp" />
    </ScrollView>

    <CheckBox
        android:id="@+id/agreementCheckBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/agreement_checkbox"
        android:textSize="16sp"
        android:textColor="@color/text_primary"
        android:paddingBottom="16dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/nextButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="@string/next_button"
        android:textSize="16sp"
        android:textColor="@color/text_on_primary"
        android:enabled="false"
        app:backgroundTint="@color/dhl_red"
        app:cornerRadius="8dp"
        android:padding="12dp" />
</LinearLayout>